# Core dependencies
numpy>=1.20.0
scipy>=1.7.0

# Visualization (required for postprocessing)
matplotlib>=3.3.0
pyvista>=0.37.0

# Testing
pytest>=6.2.0
pytest-cov>=2.12.0

# Future dependencies (to be added)
# gmsh-sdk>=4.11.0          # For unstructured mesh generation
# meshio>=5.0.0             # For mesh format conversion
# shapely>=1.8.0            # For geometry processing
# gdal>=3.0.0               # For GIS data import
# PyQt6>=6.0.0              # For GUI (alternative: streamlit)
